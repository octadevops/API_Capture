# Core
streamlit>=1.37
pandas>=2.0
plotly>=5.20
requests>=2.31
pypdf>=4.0
trafilatura>=1.8
colorama>=0.4.6
python-dotenv>=1.0

# Browser / crawling (headless). Selenium works on Streamlit Cloud with chromium.
selenium>=4.15
webdriver-manager>=4.0

# Optional multi-engine crawlers — uncomment if you have keys
# firecrawl-py>=1.0
# apify-client>=1.8
# crawl4ai>=0.4

# Trained model — transformers + torch for FinBERT inference
# Keep torch CPU-only for Cloud (smaller)
transformers>=4.40
torch --index-url https://download.pytorch.org/whl/cpu
accelerate>=0.30
scikit-learn>=1.4
datasets>=2.18  # only needed for training (models/train.py)

# Flask dashboard still supported alongside Streamlit
flask>=3.0
